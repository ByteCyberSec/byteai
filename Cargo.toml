[workspace]
resolver = "3"
members = ["crates/byteai-types", "crates/byteai-provider", "crates/byteai-tools", "crates/byteai-core", "crates/byteai-cli", "crates/byteai-lsp", "crates/byteai-ast", "crates/byteai-dap", "crates/byteai-memory"]

[workspace.package]
edition = "2024"
version = "0.1.0"
license = "MIT"
authors = ["ByteAi contributors"]
rust-version = "1.85"
repository = "https://github.com/ByteCyberSec/byteai"
homepage = "https://github.com/ByteCyberSec/byteai"
description = "ByteAi — fast autonomous coding agent: Rust core, optional power modules (LSP, DAP, memory, skills, multi-agent, routing, review)."

[workspace.dependencies]
byteai-types = { path = "crates/byteai-types" }
byteai-provider = { path = "crates/byteai-provider" }
byteai-tools = { path = "crates/byteai-tools" }
byteai-core = { path = "crates/byteai-core" }
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "rustls-tls", "charset"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
dirs = "6"
regex = "1"
futures-util = "0.3"
toml = "0.8"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
chrono = { version = "0.4", features = ["serde"] }
rusqlite = { version = "0.32", features = ["bundled"] }
