# Reference clones (Phase 0 research only — not part of the build)
research/repos/
target/
*.log
.DS_Store
.env
*.db
*.db-wal
*.db-shm
.byteai/
.checkpoints/

# Vendored reference clones (kept locally, not part of the build)
vendor/
